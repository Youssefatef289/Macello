# ماتشيللو (Matchillo Store)

واجهة متجر لحوم بالعربية — React + Vite + TypeScript + Tailwind CSS v4.

## التشغيل

```bash
npm install
npm run dev
```

- `npm run build` — بناء الإنتاج
- `npm run preview` — معاينة البناء

## المسارات

`/`, `/products`, `/cart`, `/order-confirmed`
